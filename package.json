{
  "name": "alzheimers-disease-detection-root",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "build": "npm run build --workspace=frontend"
  },
  "engines": {
    "node": ">=20.0.0 <24.0.0"
  }
}
